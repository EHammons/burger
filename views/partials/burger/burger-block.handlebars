<li class="list-group-item list-group-item-danger">
	<button class="change-devour btn btn-danger" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}EAT ME!{{else}}REMAKE?{{/if}}
	</button>

	{{burger_name}}
</li>